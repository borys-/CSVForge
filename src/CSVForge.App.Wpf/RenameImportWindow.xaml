<Window x:Class="CSVForge.App.Wpf.RenameImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zmień nazwę tabeli" Width="440" Height="180" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Background="#F6F7F9">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Nowa nazwa" FontWeight="SemiBold" />
        <TextBox x:Name="NameTextBox" Grid.Row="1" Height="32" Margin="0,8,0,0" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Anuluj" Width="90" Height="32" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Zapisz" Width="96" Height="32" IsDefault="True" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
