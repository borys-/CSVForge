<Window x:Class="CSVForge.App.Wpf.ColumnSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz kolumny" Width="460" Height="580" MinWidth="380" MinHeight="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="#F5F7FB">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" FontSize="21" FontWeight="SemiBold" />

        <Grid Grid.Row="1" Margin="0,14,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SelectionStatusText" VerticalAlignment="Center" Foreground="#667085" />
            <Button Grid.Column="1" Content="Zaznacz wszystkie" Padding="10,5" Margin="8,0"
                    Click="SelectAll_Click" />
            <Button Grid.Column="2" Content="Wyczyść" Padding="10,5" Click="ClearAll_Click" />
        </Grid>

        <Border Grid.Row="2" Background="White" BorderBrush="#D8DEE9" BorderThickness="1"
                CornerRadius="8" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ColumnsPanel" />
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Anuluj" MinWidth="90" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Zastosuj" MinWidth="100" IsDefault="True"
                    Background="#2563EB" Foreground="White" Click="Apply_Click" />
        </StackPanel>
    </Grid>
</Window>
